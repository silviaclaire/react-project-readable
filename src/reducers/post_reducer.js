import {
  GET_POSTS_BY_CATEGORY,
  GET_POSTS,
  ADD_POST,
  FETCH_POST,
  VOTE_POST,
  EDIT_POST,
  DELETE_POST
} from '../actions/post_actions'

export default function postReducer (state = [], action) {
  switch (action.type) {
    default :
      return state
  }
}